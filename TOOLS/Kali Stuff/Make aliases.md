# Make aliases
---
## Make copy aliases
---
Get xsel
```bash
sudo apt instal xsel
```

From the home directory open the `.zshrc` file

```bash
code .zshrc 
```

add alias
```bash
# make copy aliases
alias copy='xsel -ib'
```
